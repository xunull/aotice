{
  "name": "aotice",
  "version": "0.1.2",
  "private": false,
  "type": "module",
  "license": "Apache-2.0",
  "description": "处方性 CLI:审计你的 Claude Code 会话,实测参数并给出成本最优的上下文压缩阈值(alpha)。",
  "bin": {
    "aotice": "dist/cli.mjs"
  },
  "files": [
    "dist",
    "README.md",
    "LICENSE"
  ],
  "repository": {
    "type": "git",
    "url": "git+https://github.com/xunull/aotice.git",
    "directory": "packages/cli"
  },
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "test": "node --test",
    "bundle": "node build.mjs"
  },
  "devDependencies": {
    "@aotice/compaction-model": "*",
    "esbuild": "^0.28.0"
  }
}
